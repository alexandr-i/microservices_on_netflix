server.port: 8100
spring:
  application.name: discovery-service
  config:
    import: optional:configserver:http://localhost:8099
eureka.client:
  register-with-eureka: false
  fetch-registry: false
  service-url.defaultZone: http://localhost:8100/eureka
